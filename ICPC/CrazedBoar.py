

def get_range(tree, b, d):
    x, y, r = tree[0], tree[1], tree[2]
    dist = (x**2 + y**2)**.5
    

def tree_in_range():

def tree_on_border():


n = int(input())
trees = []
for i in range(n):
    line = input().split()
    trees.append([int(line[0]), int(line[1], int(line[2]])

b = input.split()
d = int(b[1])
b = int(b[0])
ranges = []
for tree in trees: